# Los scripts de Linux siempre con saltos de línea LF (si no, fallan en el servidor)
*.sh text eol=lf
install.sh text eol=lf
*.conf text eol=lf
# Binarios
*.png binary
*.jpg binary
*.apk binary
*.jar binary
